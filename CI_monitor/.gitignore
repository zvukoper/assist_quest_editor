node_modules/
out/
*.vsix
tsconfig.tsbuildinfo

# Состояние CI Monitor лежит в корне репозитория (.ci-state), а не рядом с
# расширением. Эти пути — защита от артефактов, если расширение запустят
# из каталога CI_monitor: иначе каталог состояния создастся внутри расширения
# и попадёт в коммит.
.ci-state/
MemoryAI/
